[core]
  ignorecase = false
  excludesfile = ~/.gitignore-global
  editor = vim

[user]
  email = darrinholst@gmail.com
  name = Darrin Holst

[github]
  user = darrinholst

[color]
  ui = true

[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold

[alias]
  ad = !git add -A && git st
  branch-name = !git rev-parse --abbrev-ref HEAD
  branches = for-each-ref --sort=-committerdate --format=\"%(color:blue)%(authordate:relative)\t%(color:red)%(authorname)\t%(color:white)%(color:bold)%(refname:short)\" refs/remotes
  cleanup = !git fetch -p && git for-each-ref --format '%(refname:short) %(upstream:track)' | awk '$2 == \"[gone]\" {print $1}' | xargs -r git branch -D
  co = checkout
  default-branch = !git rev-parse --abbrev-ref main > /dev/null 2>&1 && echo main || echo master
  fu = !git add -A && git commit --no-verify -qm 'FU SAVEPOINT' && git reset --hard HEAD~
  lg = log --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr by %an)%Creset' --abbrev-commit --date=relative
  squashemall = !git reset $(git merge-base $(git default-branch) $(git rev-parse --abbrev-ref HEAD))
  st = status
  unwip = !git reset HEAD~
  up = !git pull --prune && git cleanup
  upall = !"gita super up"
  wip = !git add -A && git commit -m 'wip'

[push]
  default = simple
  autoSetupRemote = true

[init]
  defaultBranch = main

[filter "media"]
  clean = git-media-clean %f
  smudge = git-media-smudge %f

[tig]
  line-graphics = utf-8
  mouse = true
  main-view-date-display = relative-compact
  main-view-author-display = email-user
  main-view-id-display = yes
  main-view-commit-title-graph = no
  vertical-split = false
  diff-context = 3

[credential]
  helper = osxkeychain

[pull]
  rebase = true

[pager]
  branch = false

[url "https://github.com/"]
  insteadOf = git@github.com:

[url "https://"]
  insteadOf = git://

[rerere]
  enabled = 1

[log]
  merges = false
